package 구현.스택큐;

public class B14719_빗물 {
    public static void main(String[] args) {
        /*
        H, W 입력받기

        스택 생성

        for(int i=0;i<w;i++){ //W만큼 반복
            int height = //입력받기

            int leftV;
            int leftI;
            if(s.isEmpty()){
                if(0초과){
                    leftV 등록
                    leftI 등록
                    스택 푸시(인덱스)
                }
            }else{
                if leftV이상 || 마지막 입력 {
                    left와 같은 것 push
                    - a[0]에 leftV 등록
                    - 0 스택 푸시

                    whlie(!s.isEmpty()){ //스택에 남은거 있을때까지
                        int now = s.pop();

                        if(leftI <= now && now <= leftI+1 && ){ //붙어있거나 left가 아니고 && height 0초과
                            sum += 차이;
                        }
                        pop();
                    }

                    다끝나면 height Push
                }else{
                    스택 푸시
                }

            }

        }
         */
    }
}
